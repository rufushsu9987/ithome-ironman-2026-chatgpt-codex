# Fixtures

- `intent.json`：宣告要驗證的 candidate、artifact/config identity、target 與 required checks。
- `observation.json`：runner 唯讀回報的 deployment、replica、check 與 traffic 狀態。

fixture 不含 secrets、tokens、credentials 或本機絕對路徑。
