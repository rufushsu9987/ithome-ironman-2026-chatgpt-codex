# Fixtures

- `intent.json`：宣告 change identity、必要 evidence 與 approval policy。
- `observation.json`：兩位不同 release owner 對同一 candidate、target 與 evidence 的公開示例核准。

兩份 JSON 都是安全的示例資料，不含 credentials、tokens 或本機路徑；epoch 數字刻意固定，讓測試可重跑。
