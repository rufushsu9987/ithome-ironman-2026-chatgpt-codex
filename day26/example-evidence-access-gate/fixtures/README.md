# Fixtures

- `intent.json` 宣告 Day 26 Evidence Access Gate 的 identity、角色、目的、欄位 allowlist 與時間政策。
- `observation.json` 是已綁定同一個 incident digest 的 access request、evidence inventory、approval 與 audit anchor。

Fixture 只供本機測試與 CLI 示範，不代表任何真實 incident 或外部授權。
