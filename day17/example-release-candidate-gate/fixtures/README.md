# Fixtures

- `intent.json`：發布前固定的 candidate、target、時間窗、必要 checks、expected artifacts 與 rollback identity。
- `observation.json`：runner 實際觀察到的 artifact、check、時間、rollback 與 approval。

兩份 fixture 都是範例資料，不包含 token、credential 或本機路徑。
