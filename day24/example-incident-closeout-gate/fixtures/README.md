# Fixtures

- `intent.json`：宣告本次 incident 的 identity、最低 impact window、critical follow-up 與核准 policy。
- `observation.json`：模擬 recovery verified、impact window、follow-up、postmortem、learning pack 與 closeout approval 都符合條件的讀回結果。

Fixture 不包含 token、credential 或外部服務連線資訊。
