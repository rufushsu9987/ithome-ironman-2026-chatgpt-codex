# Fixtures

- `intent.json`：宣告 incident identity、最低留存秒數、required evidence、access scope 與 legal hold policy。
- `observation.json`：模擬 inventory 已驗證、archive 已完成、evidence 可回讀且保留期限足夠的結果。

Fixtures 只包含示範資料，不含 credentials、token 或外部服務資訊。
